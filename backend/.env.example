PORT=3001
DATABASE_URL=postgres://jym:jym@localhost:5432/jym

# openssl rand -base64 32  — два РІЗНІ значення, не копіювати одне в обидва
JWT_SECRET=change-me
JWT_REFRESH_SECRET=change-me-too

FRONTEND_URL=http://localhost:3000
